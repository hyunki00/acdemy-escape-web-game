# 개발 일지 — 바이미디어 아카데미: 이스케이프

프로젝트 진행 과정을 날짜별로 기록합니다. **HOW**(어떻게 했는지)와 **WHY**(왜 그렇게 했는지)를 함께
남겨서, 나중에 포트폴리오나 이력서에서 설계 의도를 바로 설명할 수 있도록 합니다.

> 이 문서는 GitHub의 콜아웃 문법(`[!NOTE]`, `[!IMPORTANT]`)을 사용합니다 — GitHub 저장소에서 보면
> HOW는 파란색 박스, WHY는 보라색 박스로 자동으로 색이 입혀져 보입니다.

---

## 2026-09-22

### 01. 스터디룸 상자 신규 오브젝트 — 콤보락(회전 다이얼) UI 신규 제작

> [!NOTE]
> **HOW** — 스터디룸 책상 위에 잠긴 상자를 새로 배치. 닫힌 상자(`kind: 'lock'`)를 클릭하면 대사 →
> 콤보락 UI로 이어지는데, 기존 도어락(디지털 버튼식)과는 다른 실물 자물쇠 느낌을 위해 ▲▼로 각 자리
> 숫자를 돌리는 회전 다이얼 UI(`style: 'combo'`)를 `lock.js`에 별도 함수(`openComboLock`)로 신규
> 구현. 정답을 맞히면 배경이 열린 상자 이미지로 바뀌고, 히트박스도 `showIf`로 닫힌 상자→열린 상자
> 오브젝트로 자동 전환되며 스마트폰 아이템을 지급.

> [!IMPORTANT]
> **WHY** — 모든 잠금을 같은 디지털 키패드로 통일하면 반복적으로 느껴질 것 같아, 오브젝트(자물쇠
> 달린 상자)의 실제 생김새에 맞는 별도 UI 타입을 만들어 상호작용에 다양성을 줌.

### 02. 전체 모달 다크 테마 리스킨 + 하단 UI(소지품·이동) 이미지 에셋 기반 재설계

> [!NOTE]
> **HOW** — 기존 흰 배경 퍼즐 모달을 도어락 모달과 같은 어두운 톤(`#1c1d22`→`#0d0e11` 그라디언트)
> 으로 전체 통일하고, 선택지·입력창·피드백 색상도 다크 배경 기준으로 재조정. 이후 참고 이미지로 받은
> 이동 버튼·하단 바 배경·아이템 슬롯 PNG 에셋을 그대로 적용해, 따로 있던 이동 버튼 바와 소지품 바를
> 한 줄로 합침(왼쪽 소지품 칸, 오른쪽 이동 버튼). 이동 버튼에서 둥근 모서리 틈으로 브라우저 기본 흰
> 배경이 비치는 문제는 `background-color:transparent`로 해결.

> [!IMPORTANT]
> **WHY** — 흰 모달은 정전된 학원이라는 전체 분위기와 맞지 않아서 통일. 하단 UI는 참고 이미지 속
> "소지품+이동이 한 줄로 붙은" 구성이 실제 게임 UI 정석에 더 가까워서 그대로 반영.

### 03. 화장실 거울 퍼즐 전면 재설계 — 손수건 스크래치 기믹 (여러 차례 반복 개선)

> [!NOTE]
> **HOW** — 기존 "JUST JOKING" 워드플레이를 버리고, 선명한 붉은 글씨로 "누가 범인인 것 같아?"를
> 묻는 새 문제로 교체. 손수건 아이템이 없으면 입력 자체가 불가능하고, 있으면 캔버스에 미리 적혀있는
> 답("신지원")을 손수건으로 문질러 지워야 드러나는 스크래치 기믹(`type: 'hold'`)을 신규 구현. 구현
> 과정에서 세 가지 문제를 순차적으로 발견·수정함 — ① 텍스트를 캔버스에 직접 그린 뒤 덮개로 덮으면
> 덮개가 텍스트 픽셀 자체를 지워버려 긁어도 안 나타나는 문제 → 텍스트를 캔버스 "아래" 별도 DOM
> 레이어로 분리해 해결. ② 완료 판정을 글자 바운딩 박스처럼 좁은 영역 기준으로 잡아서 체감보다 너무
> 빨리 끝나던 문제 → 캔버스 전체 면적의 70% 이상 지워져야 완료로 판정 기준을 넓힘(`getImageData`로
> 실제 알파값을 픽셀 단위로 측정). ③ 드래그 중 커서가 캔버스 경계를 벗어나면 `mouseleave`로 긋기가
> 끊기던 문제 → `Pointer Events` + `setPointerCapture`로 전환해 경계 밖에서도 스트로크가 이어지게
> 함. 마지막으로 손수건 실사 이미지를 다듬어 스크래치 영역 전용 커스텀 마우스 커서로 적용.

> [!IMPORTANT]
> **WHY** — 스크래치 기믹은 "정답을 직접 타이핑하는" 기존 방식보다 훨씬 인터랙티브해서 게임성을
> 살리기 좋다고 판단해 시도함. 캔버스는 레이어 개념이 없다는 걸 실제로 겪으며 확인했고, 이런 시행착오
> 자체가 왜 최종 구조(DOM 분리, 전체 면적 기준, 포인터 캡처)를 택했는지 설명할 수 있는 좋은 사례.

### 04. 아이템 체계 개편 — 이미지 아이콘화, 손수건 도입(빨간펜 대체), 소진 시스템

> [!NOTE]
> **HOW** — 손전등·쪽지·스마트폰·녹슨 열쇠 아이템에 이모지 대신 실제 이미지 아이콘을 적용할 수
> 있도록 `item.image` 필드를 신규 지원(`inventory.js`). 인포데스크 의자에서 지급되던 빨간 펜을
> 흰 손수건으로 교체 — 이 아이템을 요구하던 다른 지점(마룬 도어의 진실 공개 조건)도 함께
> `handkerchief`로 갱신해 빠짐없이 연결. "쓰고 나면 사라지는" 아이템(손전등→빔프로젝터, 녹슨
> 열쇠→캐비닛)을 위해 `withItem`에 `removeItem` 필드를 신규 지원하고, 인벤토리에서 실제로
> 제거·슬롯 재배치하는 `removeInventoryItem()`을 구현. 아이템 획득 시 화면 중앙에 아이콘이 크게
> 떴다가 자동으로 사라지는 팝업과 전용 효과음도 추가.

> [!IMPORTANT]
> **WHY** — 이모지보다 직접 그린 아이콘이 훨씬 게임다워서 전환. 아이템 하나를 교체할 때 그 아이템을
> 요구하는 다른 모든 지점을 함께 찾아 갱신하지 않으면 게임이 영구히 막힐 수 있다는 걸 계속 유의해야
> 했음 — 실제로 마룬 도어 쪽을 놓칠 뻔한 걸 검증 중 발견.

### 05. 인포데스크 컴퓨터 로그인 → 가짜 뉴스 사이트 퍼즐 신규 구현

> [!NOTE]
> **HOW** — 기존 "빨간 펜으로 모니터 먼지 닦기" flavor 상호작용을 진짜 퍼즐로 교체. `puzzle.js`에
> `login`(ID/PW 입력 + 힌트 버튼으로 정답 직접 공개) · `newsfeed`(기사 여러 개를 나열하는 읽기 전용
> 타입, 기존 `info`와 확인 로직 공유) 두 타입을 신규 추가. 로그인 성공 시 `followUp` 체인으로
> 이어지는 뉴스 사이트 화면에는 지시받은 3개 기사(신원 미상 강사 체포, 엘리베이터 감금 살인,
> 연쇄살인범 제보)를 배치. ID 입력란은 정답을 미리 채워두고, 한 번 풀면 재방문 시 로그인 폼이 아니라
> 뉴스 화면으로 바로 열리도록 기존 `followUp` 재오픈 로직을 그대로 재사용.

> [!IMPORTANT]
> **WHY** — 인포데스크는 아이템만 놓여있고 실제 "풀 거리"가 없던 공간이라, 이 자리에 진짜 퍼즐을
> 만들어 채움. 가짜 뉴스 기사는 "강사님의 장난이었다"는 최종 반전 전에 긴장감을 끌어올리는 떡밥
> 역할.

### 06. 화장실 캐비닛 → 차단기함 연결, Lights Out 스타일 미니게임 신규 제작

> [!NOTE]
> **HOW** — 녹슨 열쇠로 캐비닛을 열면(`setState: 'cabinetOpen'`) 안에서 예전에 위치를 못 정해
> 미뤄뒀던 차단기함 퍼즐(`p_breaker`)이 등장하도록 연결. 차단기함 자체는 기존의 단순 빈칸 코드
> 문제를 버리고, 3×3 차단기 버튼 + 상하좌우로 인접한 12개의 전선으로 이루어진 "Lights Out" 방식
> 미니게임으로 새로 설계(`type: 'breakerbox'`). 차단기를 누르면 인접한 전선이 전부 토글되고, 모든
> 전선이 켜져야 완료. 그래프 이분 색칠(체스판 패턴) 원리를 이용해 대각선 3개(좌상단·중앙·우하단)
> 버튼을 고장내고도 가장자리 중간 4개만 누르면 항상 풀리도록 수학적으로 설계.

> [!IMPORTANT]
> **WHY** — 차단기함이라는 오브젝트 이름과 실제 문제 내용이 그동안 무관했는데, 실제 차단기 패널
> 생김새를 반영한 퍼즐로 바꿔 오브젝트와 퍼즐이 서로 설명되게 함. 고장난 버튼이 있어도 항상 풀 수
> 있어야 한다는 요구사항을, 그래프 이론(이분 그래프의 2색 색칠)으로 아예 수학적으로 보장되는 구조를
> 찾아 해결 — 케이스를 하나하나 검증하는 대신 애초에 풀리지 않는 조합이 나올 수 없게 설계함.

### 07. 전원 시스템 정비 — 차단기함과 연동, 엘리베이터 문 전원 게이팅, 상태별 배경 전환

> [!NOTE]
> **HOW** — 차단기함을 풀면 기존에 이미 만들어져 있던 `onBreakerSolved()`(→ `state.power = true`)
> 로 자연스럽게 이어지도록 연결. 엘리베이터 문(`elevatorCall`)에는 `requiresPower` 플래그를 추가해,
> 전원이 꺼진 동안은 눌러도 암호창 자체가 열리지 않고 안내 대사만 나오도록 막음(이전엔 이 체크가
> 빠져 있었음). 전원이 켜지면 엘리베이터 앞 배경 전체가 "▲1" 표시등이 들어온 새 이미지로 바뀌도록
> `background`를 상태 함수로 전환(기존 히트박스 좌표는 새 이미지에도 그대로 맞아 좌표 수정 불필요).
> 문이 실제로 열리는 순간에는 기존 범용 도어락 효과음 대신 엘리베이터 "땡" 소리가 나도록 잠금별
> `openSound` 필드를 신규 지원.

> [!IMPORTANT]
> **WHY** — 전원 꺼진 상태에서도 엘리베이터 키패드가 열리는 건 명백한 논리적 구멍이라 우선 막음.
> 하나의 잠금 UI 시스템을 여러 오브젝트(문·엘리베이터)가 공유하다 보니, 문 효과음을 그대로 쓰면
> 몰입이 깨져서 오브젝트별로 다른 "열림음"을 줄 수 있게 확장함.

### 08. 오디오 시스템 확장 — 상대 볼륨 배율, 효과음별 개별 조절

> [!NOTE]
> **HOW** — 마스터 볼륨 슬라이더는 유지하되, 그 위에 곱해지는 배율을 세분화. `SFX_VOLUME_MULTIPLIER`
> (효과음 전체 기본 1.3배), `BGM_VOLUME_MULTIPLIER`(배경음악 0.8배), `SFX_VOLUME_OVERRIDES`(특정
> 파일만 다른 배율 — 유리 깨지는 소리 0.8배)를 `main.js`에 신규 도입. 공용 `sfxAudio` 재생 함수
> (`playSfx`)는 재생 시점마다 해당 파일에 맞는 배율을 계산해 적용하고, 슬라이더 값이 바뀔 때도
> BGM/도어락/걷기 등 모든 `<audio>`에 일괄 재적용되도록 `applyVolume()`을 재작성.

> [!IMPORTANT]
> **WHY** — 사용자가 소리를 직접 녹음·조절해 보내는 과정에서 파일 간 상대적인 크기 차이가 생기는 건
> 자연스러운 일이라, 파일을 다시 편집해서 보낼 필요 없이 코드 쪽 배율만으로 균형을 맞출 수 있게
> 설계함.

### 09. 게임 시작 오프닝 대사 및 findHotspot 버그 수정

> [!NOTE]
> **HOW** — 게임 로드 시 자동으로 대화창이 뜨도록 `main.js`에 오프닝 대사(배열, 현재 3줄)를 추가 —
> 기존 대화창이 이미 배열을 지원하고 있어 줄 수를 늘리거나 줄여도 그대로 순서대로 넘어감. 별개로,
> 같은 id를 상태별로 여러 개 등록해둔 오브젝트(예: 잠긴/열린 캐비닛)에서, 클릭 시 대사를 찾는
> `findHotspot()`이 `showIf` 조건을 무시하고 배열의 첫 항목만 반환하던 버그를 발견해 수정 — 이제
> 항상 현재 상태에 맞는 항목만 찾도록 필터링을 추가함.

> [!IMPORTANT]
> **WHY** — 오프닝 대사는 플레이어에게 상황을 설명해줄 최소한의 진입점이 필요해서 추가(텍스트는
> 추후 직접 수정 예정). `findHotspot` 버그는 상자·거울처럼 id를 다르게 준 경우엔 우연히 드러나지
> 않다가, 캐비닛처럼 id를 재사용한 곳에서 실제로 걸려 발견됨 — 같은 종류의 버그가 앞으로 또
> 재발하지 않도록 개별 증상을 고치는 대신 원인 함수(`findHotspot`) 자체를 근본적으로 고침.

---

## 2026-09-21

### 01. 씬 전환 페이드 인/아웃 + 이동 효과음 추가

> [!NOTE]
> **HOW** — 방을 이동할 때(`goRoom()`) 씬을 즉시 교체하는 대신, `#sceneArt`에 `opacity` 트랜지션
> (0.5초)을 걸어 페이드아웃 → 화면 교체 → `requestAnimationFrame`으로 다음 프레임에 클래스 제거 →
> 페이드인 순서로 총 1초짜리 전환을 구현. 이동이 시작되는 시점에 `walk.mp3`를 재생.

> [!IMPORTANT]
> **WHY** — 방이 즉시 툭 바뀌는 것보다 짧게라도 암전 구간이 있는 편이 공간 이동의 무게감을 살려줌.
> 페이드 중간에 걷는 소리를 겹쳐서 "장면 전환"이 아니라 "실제로 걸어서 이동하는" 느낌을 강화.

### 02. 대화창 상호작용 방식 전면 개편 — 시간 자동 진행 → 클릭 진행

> [!NOTE]
> **HOW** — 글자 수에 비례해 자동으로 사라지던 방식을 버리고, 대사 끝에 ▽ 표시를 붙여 클릭해야
> 다음 줄로 넘어가거나(마지막 줄이면) 닫히도록 `showDialogue()`를 재작성. 배열을 넘기면 여러 줄을
> 순서대로 보여줄 수 있게 확장. 이후 "화면(배경 이미지) 아무 곳을 클릭해도 넘어가야 한다"는 요청에 맞춰
> 진행 로직을 `#sceneWrap` 레벨로 옮겼다가, 사물을 클릭해 대화가 막 열리는 순간 그 클릭 이벤트가
> `sceneWrap`까지 버블링되어 방금 연 대화를 같은 클릭에서 즉시 닫아버리는 버그를 발견해
> `e.stopPropagation()`으로 수정. 대화창이 떠 있는 동안엔 다른 사물에 마우스를 올려도 하이라이트가
> 뜨지 않도록 `body.dialogue-active` 클래스로 `pointer-events`를 차단.

> [!IMPORTANT]
> **WHY** — 대사를 다 읽기 전에 자동으로 넘어가거나, 반대로 짧은 대사인데도 한참 기다려야 하는 게
> 답답하다는 피드백이 있어 전적으로 플레이어 페이스에 맡기는 구조로 바꿈. 자기 자신을 닫아버리는
> 버그는 이벤트 버블링을 놓쳐서 생긴 전형적인 실수라, 같은 클릭 사이클 안에서 새로 연 UI로 이벤트가
> 잘못 전파되지 않도록 항상 점검해야 한다는 걸 다시 확인함.

### 03. 도어락 UI 신규 구현 (실물 참고 이미지 기반)

> [!NOTE]
> **HOW** — 실제 도어락 사진을 레퍼런스로 받아, 검은 배경 + 파란 글로우 텍스트의 `lock-modal` 스킨을
> 새로 제작. 잠긴 문을 처음 클릭하면 이제 단서 체크리스트 없이 곧바로 암호 입력 UI로 들어가도록 구조를
> 단순화. 추가로 "문 클릭 → 화면 중앙에 도어락 이미지 팝업 + 대사 동시 표시 → 대사를 다 넘기면 팝업이
> 사라지며 암호창으로 전환"되는 인트로 연출을 `ROOMS.*.connections[].introImage`/`introLine` 필드로
> 데이터화해 다른 문에도 재사용 가능하게 만듦. 키패드 버튼음·정답음·오답음 효과음 3종을 연결.

> [!IMPORTANT]
> **WHY** — 기존의 "단서를 다 모아야 키패드가 뜨는" 체크리스트 방식은 이미 코드 조각을 다 모았는지
> 확인하는 절차가 한 단계 더 끼어 있어 번거로웠고, 실물 도어락 사진 대비 UI가 밋밋했음. 인트로 연출을
> 재사용 가능한 데이터 필드로 뽑아둔 건, 앞으로 다른 잠긴 문에도 같은 패턴을 쉽게 적용하기 위함.

### 04. 잠금 관련 텍스트 전부 data.js로 중앙화

> [!NOTE]
> **HOW** — "틀렸어요, 다시 시도해보세요" 등 `lock.js`에 하드코딩돼 있던 문구들을 `data.js`의
> `LOCK_TEXT` 객체(title/subtext/wrong/success)로 옮기고, 개별 `LOCKS` 항목에 같은 이름의 필드를
> 넣으면 그 잠금만 다른 문구로 재정의되도록 설계.

> [!IMPORTANT]
> **WHY** — 화면에 보이는 문구를 고치려 할 때마다 로직 파일(`lock.js`)까지 뒤져야 하는 건 비효율적.
> 다른 퍼즐 데이터들처럼 텍스트는 전부 `data.js` 한 곳에서 관리되도록 일관성을 맞춤.

### 05. 퍼즐 완료 흐름 버그 수정 + 2단계 체인 재오픈 로직 개선

> [!NOTE]
> **HOW** — "확인"을 눌러 정답을 맞혀도 모달이 자동으로 닫히지 않고 이전 화면(질문+버튼) 위에 완료
> 메시지만 얹힌 채 계속 떠 있던 버그를 발견해, 완료 메시지를 잠깐 보여준 뒤 자동으로 닫히도록 수정.
> 2단계로 구성된 퍼즐(모니터)이 이미 완전히 풀린 뒤에는, 다시 클릭해도 1단계(빈칸 문제)가 아니라 항상
> 2단계(날짜 질문) 화면이 열리도록 재오픈 로직을 변경. 질문형(`info`) 모달은 폭을 좁게(440px) 줄임.

> [!IMPORTANT]
> **WHY** — 모달이 안 닫히는 버그 때문에 완료 후 화면이 텍스트에 비해 불필요하게 넓어 보이는 문제가
> 있었는데, 단순히 문구를 지우는 대신 "왜 안 닫히는지"부터 짚어 근본 원인을 고침. 1단계 문제는 한 번
> 풀면 다시 볼 필요가 없고, 2단계(날짜) 정보는 계속 열람 가능한 편이 자연스러워 재오픈 시 기본 화면을
> 다르게 분기함.

### 06. 오브젝트별 개별 효과음 + 마스터 볼륨 컨트롤

> [!NOTE]
> **HOW** — 엘리베이터 앞 철문, 화장실 캐비닛에 각각 클릭 효과음을 연결. 오브젝트마다 `<audio>` 태그를
> 늘리는 대신, 공용 `<audio id="sfxAudio">` 하나와 `playSfx(src)` 함수로 어떤 hotspot이든
> `sound: 'Sound/파일명.mp3'` 필드 하나만 추가하면 재생되도록 범용화. 설정 모달에 볼륨 슬라이더를
> 추가해 게임 안의 모든 `<audio>`(BGM·도어락·걷기·오브젝트 효과음)의 볼륨을 한 번에 조절하도록 구현.

> [!IMPORTANT]
> **WHY** — 효과음이 하나둘 늘어날 때마다 태그와 재생 코드를 매번 새로 추가하는 건 확장성이 떨어져서,
> 범용 재생기 하나로 통일. 볼륨 조절 수단이 음소거 버튼 하나뿐이었는데, 세밀하게 소리 크기를 맞추고
> 싶다는 요청에 맞춰 슬라이더를 추가.

### 07. 인벤토리 슬롯 UI 개편 — 빈 칸 미리 배열

> [!NOTE]
> **HOW** — 아이템을 주울 때마다 칸이 새로 생기던 방식에서, 게임 시작 시 `INVENTORY_SLOTS`(4)개의
> 빈 칸을 미리 그려두고 아이템을 주울 때마다 첫 번째 빈 칸을 채우는 방식으로 변경. 채워진 칸은 기존
> 골드 그라디언트, 빈 칸은 점선 테두리의 은은한 슬롯으로 구분.

> [!IMPORTANT]
> **WHY** — 참고 이미지 속 "미리 배열된 빈 칸들"이 실제 인벤토리 UI의 정석적인 느낌을 줘서, 지금
> 몇 개를 더 모아야 하는지 한눈에 보이도록 개선.

### 08. 빔프로젝터 "밝힌 뒤" 대사 유지 버그 수정

> [!NOTE]
> **HOW** — 손전등으로 빔프로젝터를 한 번 밝히면 배경 이미지는 영구히 바뀌는데, 대사는 최초 1회만
> "밝힌 뒤" 문구를 보여주고 이후엔 다시 "밝히기 전" 기본 대사로 돌아가던 버그를 수정. `setState`로
> 상태가 이미 영구적으로 켜져 있다면, 이후 클릭에서도 계속 "밝힌 뒤" 대사를 반복 표시하도록 분기 추가.

> [!IMPORTANT]
> **WHY** — 배경(시각 정보)과 대사(텍스트 정보)가 서로 다른 상태를 가리키면 플레이어가 혼란스러움.
> 두 가지가 항상 같은 상태를 반영하도록 맞춤.

### 01. 남은 4개 방 히트박스 배치 완료 (화장실 · 엘리베이터 문 · 인포데스크 · 스터디룸)

> [!NOTE]
> **HOW** — 어제와 같은 방식(투명 PNG 컷아웃 → OpenCV 윤곽선 추출 → 배경 위에 겹쳐서 확인 → 좌표
> 반영)으로 나머지 방들을 마저 배치. 화장실은 거울(퍼즐)·캐비닛·칸막이, 엘리베이터 앞은 엘리베이터
> 문 자체를 `kind: 'lock'`으로 추가해 기존 "엘리베이터 안" 이동 버튼을 대체, 인포데스크는
> 모니터·로고·의자, 스터디룸은 화분·팻말·책상을 배치.

> [!IMPORTANT]
> **WHY** — TODO에 남아있던 "엘리베이터 안: 이동 버튼 대신 이미지 속 문을 직접 클릭해서 타는 방식으로
> 전환" 항목을 겸사겸사 해결. 이동 버튼과 클릭형 문이 공존하면 같은 목적지로 가는 경로가 두 개라
> 혼란스러워서, 이미지 클릭으로 완전히 대체함.

### 02. 대화바(대사창)가 이동 버튼을 가리는 문제 수정

> [!NOTE]
> **HOW** — `navOverlay`(이동 목적지 버튼들)와 `dialogueBar`(대사)가 각자 씬 하단에 독립적으로
> `position:absolute; bottom:0`으로 겹쳐 있던 구조를, `#bottomStack`이라는 컨테이너로 묶고
> `flex-direction: column-reverse`로 쌓는 방식으로 변경.

> [!IMPORTANT]
> **WHY** — 이동 버튼을 펼친 상태에서 사물을 클릭하면 대사가 버튼 위에 그대로 겹쳐져 버튼 텍스트가
> 안 보이는 문제가 있었음. 대사가 항상 위쪽에 오도록 쌓는 구조로 바꿔 겹침 자체를 근본적으로 없앰.

### 03. 퍼즐 코드 문법을 자바 → 자바스크립트로 전면 전환

> [!NOTE]
> **HOW** — 8개 퍼즐의 `int`/`System.out.println`/`String`/`int[]` 등을 `let`/`console.log`/
> 배열 리터럴/`for...of` 등 JS 문법으로 교체. 빈칸형 문제 중 정답이 여러 문법으로 표현 가능한 경우
> (`i++`/`i += 1`/`i = i + 1` 등)를 대비해 `answer`가 배열도 받을 수 있도록 채점 로직을 일반화하고,
> 공백·세미콜론 유무를 무시하는 `normalizeCode()`를 추가.

> [!IMPORTANT]
> **WHY** — 게임 엔진 자체는 처음부터 JS였지만, 화면에 보여주는 퀴즈 문제만 자바 문법으로 되어있어
> 불일치가 있었음. 이후 스토리 개편으로 코드 퀴즈 자체를 다른 형식으로 바꾸긴 했지만, 여러 정답을
> 인정하는 채점 구조는 지금도 그대로 재사용 중.

### 04. 스토리 전면 개편 — "강사님의 장난" 미스터리 + 아이템 체인 시스템 신규 구현

> [!NOTE]
> **HOW** — 기존의 개별 코드 퀴즈 나열 방식을, "정전된 학원에서 깨어난 주인공이 흔적을 따라가다 보면
> 사실 담당 강사님이 벌인 장난이었다는 걸 알게 되는" 미스터리 구조로 전면 개편. `navigation.js`의
> flavor 클릭 로직에 `grantItem`(첫 상호작용 시 아이템 획득)·`withItem`(특정 아이템 보유 시에만
> 나오는 1회성 대사+효과) 두 필드를 범용으로 추가. 빨간 펜·손전등·안경 케이스 3개 아이템을 모으면
> 엘리베이터 앞 마룬 도어에서 진실이 공개되며 전원이 자동 복구되도록 구현. 실제 존재하지 않는
> 사물(사물함·게시판 등)을 요구하던 `LOCKS`도 실제 배치된 오브젝트 기준으로 정리.

> [!IMPORTANT]
> **WHY** — 낱개 코드 퀴즈만 풀고 넘어가는 구조는 문제 하나하나가 서로 연결되지 않아 "게임"이라기보단
> "퀴즈 모음집"에 가까웠음. 정전·비상등 등 이미 만들어둔 분위기를 살려 하나의 완결된 이야기로 묶고,
> 모든 상호작용 오브젝트가 퀴즈일 필요는 없다는 점에 착안해 탐색·수집 중심의 아이템 체인을 도입해
> 몰입감과 플레이 다양성을 함께 높임.

### 05. 실제 퍼즐 3개 콘텐츠 제작 + 객관식(`choice`) 타입 신규 추가

> [!NOTE]
> **HOW** — 스토리에 맞춰 모니터(도입부, 객관식) · 거울(중반, 뒤집어 읽기 워드플레이 "JUST JOKING")
> · 호출 패널(최종, 객관식으로 범인 확정) 3개를 새로 작성. `puzzle.js`에 없던 객관식 문제 타입을
> 추가하고(선택지 버튼 클릭 → 채점 → 정답 시 초록 강조), 그 외 flavor 성공 메시지를 하드코딩값 대신
> `successMsg` 필드로 일반화.

> [!IMPORTANT]
> **WHY** — 빈칸/출력 예측 두 형식만으로는 "낯익은 존재가 지켜보고 있다"처럼 서술형 떡밥을 자연스럽게
> 묻기 어려워서, 객관식이라는 세 번째 형식이 필요했음. jsdom으로 정답/오답/대소문자 무시까지 실제
> 채점 로직을 돌려서 검증 후 반영.

### 06. 모니터 퍼즐을 실제 강의 정보 기반 2단계 체인 퍼즐로 재설계

> [!NOTE]
> **HOW** — `puzzle.js`에 "1단계를 맞히면 자동으로 2단계가 이어서 열리는" 체인 구조(`followUp`
> 필드)를 새로 구현. 1단계는 `[IBM x RedHat] AI ___ - AX Academy 8기`에서 "Transformation"
> 빈칸 채우기, 2단계는 "AX Academy 8기 첫 오리엔테이션 날짜"를 숫자 8자리(YYYYMMDD)로 입력받는
> 문제로, 이 숫자가 그대로 본강의실 문의 키패드 코드가 됨. 실제 오리엔테이션 날짜는 아직 미확정이라
> `00000000`으로 임시 처리해둠(추후 실제 날짜로 교체 필요).

> [!IMPORTANT]
> **WHY** — 실제로 수강 중인 강의(IBM x RedHat AX Academy 8기)와 관련된 문제를 넣어 게임에 개인적인
> 의미를 담고 싶었음. 정답을 코드 조각으로 바로 공개하는 대신, 한 번 더 생각해야 하는 2단계 힌트
> 문제로 이어지게 해서 "문 앞의 진짜 관문"이라는 긴장감을 살림.

### 07. 스토리 · 퍼즐 · 단서 정리 Word 문서 제작

> [!NOTE]
> **HOW** — `data.js`에 실제로 반영된 내용을 기준으로, 전체 스토리(3막 구조) · 권장 플레이 순서(12단계
> 표) · 퍼즐 3개 상세 · 아이템 체인 표 · 분위기용 오브젝트 목록 · 잠금 구조 · 보류 중인 항목까지
> 7개 섹션으로 정리한 `.docx` 문서를 생성. LibreOffice로 PDF 변환 후 페이지 이미지를 직접 확인해
> 레이아웃 깨짐 없는지 검수.

> [!IMPORTANT]
> **WHY** — 스토리와 퍼즐이 여러 차례 대화를 거치며 계속 바뀌다 보니, 지금 게임에 실제로 뭐가
> 들어있는지 한눈에 정리된 문서가 필요했음. 포트폴리오 자료로도 그대로 활용 가능하도록 별도 파일로
> 남겨둠.

### 08. 아이템 스왑 및 빔프로젝터 "+22" 연출 (SVG 발광 → 실사 배경 교체로 최종 전환)

> [!NOTE]
> **HOW** — 본강의실 의자(손전등)·인포데스크 의자(빨간 펜) 아이템을 서로 맞바꿈. 빔프로젝터 반응
> 연출은 처음엔 SVG로 화면 중앙에 원형 그라데이션 스포트라이트 + "+22" 텍스트를 얹는 방식으로
> 구현했다가, 실제로 조명이 켜진 완성 배경 이미지(`classroom-lit.png`)를 받아 `ROOMS.classroom`의
> `background`를 상태에 따라 이미지를 갈아끼우는 함수로 바꿈. 배경 교체 타이밍도 원래는 대사가 다
> 끝난 뒤였는데, 대사가 뜨는 것과 동시에 바로 바뀌도록 조정.

> [!IMPORTANT]
> **WHY** — SVG로 흉내낸 조명 효과보다 실제로 그려진 배경 이미지 쪽이 퀄리티가 훨씬 높아서, 있는
> 리소스를 최대한 활용하는 쪽으로 방향을 바꿈. 대사와 배경 교체 사이에 텀이 있으면 "손전등을 비추자
> 빛이 들어온다"는 대사 내용과 실제 화면 변화가 따로 노는 느낌이 들어서, 두 가지가 동시에 일어나도록
> 맞춤.

### 09. UI 가독성 개선 — 소지품 바 확대, 퍼즐 모달 확대

> [!NOTE]
> **HOW** — 소지품 바 높이(44px→76px)와 아이템 아이콘 크기(30px→48px)를 키우고 그림자를 추가.
> 퍼즐 모달 폭을 400px 고정에서 `min(640px, 92vw)`로 넓히고, 코드박스를 포함한 텍스트 크기를 전반적
> 으로 한 단계씩 키움. 빈칸형 문제는 문장 안에 입력창을 끼워넣던 방식(가로 스크롤 유발)에서, 빈칸
> 자리엔 강조 표시만 두고 정답은 문장 아래 별도 입력줄에 쓰도록 분리.

> [!IMPORTANT]
> **WHY** — 플레이 화면을 실제로 캡처해서 보니 소지품 아이콘이 잘 안 보이고, 문제 모달은 문장 한 줄이
> 좌우 스크롤바까지 생길 정도로 좁았음. 실제 스크린샷 기반 QA로 발견한 문제들이라, 눈으로 확인하며
> 고치는 과정의 중요성을 다시 확인함.

### 01. 복도(우) · 엘리베이터 앞 히트박스 배치 시작

> [!NOTE]
> **HOW** — 포토샵에서 알파채널 투명 PNG로 오려낸 사물 컷아웃(원본 캔버스 크기 유지)을 받아 OpenCV로
> 윤곽선을 자동 추출, 배경 이미지 대비 퍼센트 좌표 다각형으로 변환해 `data.js`의 `hotspots`에 반영.
> 복도(우)에 바닥 · 비상구 표시등 · "05 CLASS ROOM" 팻말(전부 flavor) 3개, 엘리베이터 앞에 문 ·
> 정수기(flavor) · 호출 버튼(`callpanel`) 3개를 배치. 적용 전 항상 배경 이미지 위에 좌표를 겹쳐 그려
> 실제 위치와 맞는지 먼저 확인 후 반영.

> [!IMPORTANT]
> **WHY** — 손으로 좌표를 어림잡아 입력하면 오차가 크고 반복 수정이 필요한데, 사물 윤곽을 그대로 딴
> 컷아웃에서 자동으로 다각형을 뽑으면 실제 사물 모양과 거의 일치하는 히트박스를 빠르고 정확하게 만들
> 수 있음. 겹쳐보기 단계를 항상 거치는 이유는, 좌표를 눈으로 확인하지 않고 바로 반영했다가 어긋나면
> 플레이 테스트 중에야 발견하게 되어 비효율적이기 때문.

### 02. 호출 버튼 상호작용 로직 개선 (전원 상태별 대사 분기)

> [!NOTE]
> **HOW** — 호출 버튼(`callpanel`)에 `lineOff`(전원 꺼짐용) · `line`(전원 켜짐 직후용) 두 대사를
> 추가. 클릭 위임 로직에서 전원이 꺼져 있으면 클릭할 때마다 `lineOff` 대사만 표시(자유 이동 모드와
> 무관하게 항상 동작), 전원이 켜진 뒤에는 최초 1회만 `line` 대사를 보여주고 이어서 퍼즐을 열며, 이후
> 클릭부터는 대사 없이 바로 퍼즐이 열리도록 분기. `lock.js`의 `openCallPanel()`은 퍼즐을 여는 역할만
> 남기고 기존 전원 꺼짐 모달 분기는 제거.

> [!IMPORTANT]
> **WHY** — 기존에는 `DEBUG_FREE_ROAM` 체크가 먼저 걸려 있어 호출 버튼을 눌러도 아무 반응이 없었음.
> 전원이 꺼진 상태에서도 버튼을 눌렀을 때 최소한의 피드백(대사)은 항상 주는 것이 자연스러운
> 상호작용이라고 판단해, flavor 사물처럼 디버그 모드와 무관하게 항상 반응하도록 만듦. 전원이 켜진
> 뒤에는 다른 퍼즐 사물과 동일하게 "최초 1회 대사 → 퍼즐" 패턴을 맞춰 게임 전체의 상호작용 규칙을
> 일관되게 유지.

### 03. 화면 레이아웃 풀블리드 전환 및 프레임 너비 동적 계산

> [!NOTE]
> **HOW** — `#frame`을 가운데 떠 있는 카드 형태(`width:min(1440px, 94vw)`, 둥근 모서리, 그림자)에서
> 브라우저 창을 꽉 채우는 형태(`width:100%`, `height:100vh`)로 변경. 이후 창이 가로로 넓어질수록
> 배경 이미지(16:9)에 레터박스가 커지는 부작용이 발견돼, `navigation.js`에 `fitFrameWidth()`를
> 추가함. 상단바 · 캡션 · 이동버튼 · 소지품바처럼 높이가 고정된 영역을 뺀 "씬에 실제로 쓸 수 있는
> 높이"를 구한 뒤, 그 높이에 이미지 비율(16:9)을 곱해 프레임의 이상적인 너비를 계산하고 화면 너비를
> 넘지 않는 선에서 적용. 방을 이동하거나 창 크기가 바뀔 때마다 재계산되도록 `render()`와
> `resize` 이벤트에 연결.

> [!IMPORTANT]
> **WHY** — 카드형 레이아웃은 바깥에 어두운 페이지 배경이 그대로 보여 화면이 꽉 찬 느낌을 주지
> 못한다는 피드백이 있었음. 다만 단순히 프레임을 화면 전체 너비로 늘리기만 하면, 씬의 가로세로
> 비율이 배경 이미지 비율과 달라지면서 오히려 씬 안쪽에 더 큰 레터박스(여백)가 생기는 역효과가 남.
> 이미지를 잘라내는(`cover`) 대신 비율을 유지하며 프레임 크기 자체를 이미지 비율에 맞추는 방식을
> 택해, 9/14에 정한 "그림 전체가 항상 온전히 보여야 한다"는 원칙을 지키면서도 여백을 최소화함.

### 04. 더블클릭 시 화면 전체 선택되는 문제 수정

> [!NOTE]
> **HOW** — `body`에 `user-select:none`(+ `-webkit-user-select:none`, 탭 하이라이트 제거용
> `-webkit-tap-highlight-color:transparent`)을 추가. 단, 퍼즐 입력창 · 키패드 표시 등 텍스트를
> 실제로 입력/선택해야 하는 `input`, `textarea`에는 `user-select:text`로 예외를 둠.

> [!IMPORTANT]
> **WHY** — 게임 화면을 빠르게 여러 번 클릭(더블클릭)하면 브라우저 기본 동작으로 화면 전체 텍스트가
> 파랗게 선택되는 현상이 발생해 몰입을 깸. 포인트앤클릭 게임 특성상 사물을 빠르게 반복 클릭하는
> 경우가 잦을 수밖에 없어, 입력 필드를 제외한 전체 영역의 텍스트 선택을 막아 근본적으로 재발을
> 방지함.

---

## 2026-09-09

### 01. 기획서(포트폴리오용) 작성

> [!NOTE]
> **HOW** — 프로젝트 이름, 개요, 게임 구조(상태 관리 · 장면 렌더링 · 인터랙션 · 퍼즐 · 잠금·코드 조합 ·
> 내비게이션 · 엔딩 7개 시스템)를 **"구성 → 작동 방식"** 형식으로 정리한 기획서를 Word 문서로 작성. 이후
> 그래픽 자산 파이프라인(실사 촬영 → AI 이미지 생성 → Photoshop 보정)과 오디오 자산 계획(BGM: 생성형 AI,
> SE: 저작권 없는 무료 음원) 섹션을 추가로 보강함.

> [!IMPORTANT]
> **WHY** — 프로젝트가 진행되는 동안 방향을 잃지 않도록, 개발에 본격적으로 들어가기 전에 스스로
> "무엇을 만들 것인지 · 어떤 구조로 만들 것인지"를 먼저 명확히 정의해두기 위함. 미리 정리해두면 이후
> 기능을 추가하거나 수정할 때마다 전체 방향과 어긋나는지 바로 판단할 수 있고, 완성 후에는 포트폴리오
> 문서로도 바로 활용할 수 있음.

### 02. 게임 스크립트 파일을 역할별로 분리

> [!NOTE]
> **HOW** — 하나의 HTML 파일 안에 인라인으로 들어있던 자바스크립트 전체(약 670줄)를 아래 8개 파일로 분리함.
>
> | 파일 | 역할 |
> |---|---|
> | `data.js` | 상태(state) + 퍼즐 · 잠금 · 방 데이터 |
> | `draw.js` | SVG 배경 · 사물 드로잉 함수 |
> | `ui.js` | 모달 열고 닫기 등 공통 UI |
> | `puzzle.js` | 퀴즈(퍼즐) 출제 · 채점 |
> | `lock.js` | 잠금 · 키패드 · 전원/호출 처리 |
> | `inventory.js` | 아이템 획득 · 보유 확인 |
> | `navigation.js` | 클릭 이벤트 위임, 방 이동, 화면 렌더링 |
> | `main.js` | 타이머 · 설정 · 재시작 · 엔딩, 게임 시작 진입점 |

> [!IMPORTANT]
> **WHY** — 코드가 한 파일에 다 몰려 있으면 특정 기능(퀴즈, 이동, 아이템 처리 등)을 고치려 할 때마다
> 670줄 전체를 스크롤하며 찾아야 해서 관리가 점점 어려워짐. 개발자로서 코드를 역할별로 구분해두면,
> 특정 기능을 수정할 때 해당 파일만 열어보면 되고, 앞으로 기능이 늘어나도 구조가 흐트러지지 않아
> 유지보수가 쉬워짐. 이번 분리로 **코드의 구분과 관리, 보수의 용이성**을 확보하는 것이 목적.

---

## 2026-09-11

### 01. 실제 학원 전개도 기반으로 이동 동선 수정

> [!NOTE]
> **HOW** — 사용자가 촬영한 실제 학원 비상대피도(전개도)를 참고해 방 연결 구조를 수정. 스터디실 연결을
> 복도(앞) → 복도(뒤)로 이동하고, 관련 퍼즐 요소(게시판, 메모, 문)도 함께 복도(뒤) 화면으로 옮김. 본
> 강의실 설명에 실제 표지판 문구("5강의실")를 반영.

> [!IMPORTANT]
> **WHY** — 실제 대피도를 보면 스터디룸은 엘리베이터·인포메이션과 같은 위쪽 구역에 있고, 화장실은
> 반대쪽 아래 구역에 있음. 게임 속 동선이 실제 공간 구조와 어긋나 있으면 몰입감이 떨어지고, 이후
> 실사 기반 이미지로 전환했을 때 배경과 동선이 맞지 않아 어색해짐. 미리 구조를 맞춰두면 이미지 자산이
> 들어와도 자연스럽게 연결됨.

### 02. 오디오 자산 제작 착수 — 기본 BGM 완성

> [!NOTE]
> **HOW** — 생성형 AI 음악 도구로 게임 전체에 깔릴 기본 배경음악(BGM)을 제작 완료. 실사 이미지의
> 게임 그래픽화 작업은 현재 진행 중.

> [!IMPORTANT]
> **WHY** — 그래픽 자산보다 먼저 완성 가능한 BGM을 확보해, 이후 이미지 자산이 완성되는 대로 바로
> 오디오까지 함께 적용할 수 있도록 준비 순서를 앞당김.

### 03. 이미지 기반 렌더링 시스템 최초 구현 및 복도 배경 적용

> [!NOTE]
> **HOW** — `renderImageScene()` 함수를 추가해 SVG 대신 실제 이미지 + 퍼센트 좌표 히트박스로 방을
> 그릴 수 있는 구조를 만들고, AI로 변환한 복도 이미지를 복도(앞) 방에 처음 적용. 마우스를 올리면
> 노란 테두리로 하이라이트되는 hover 효과도 함께 구현. 이후 생성 오류가 발견된 이미지를 두 차례
> 교체본으로 재적용함.

> [!IMPORTANT]
> **WHY** — 기획서 3.2에 미리 세워둔 "SVG → 이미지 전환" 계획을 실제로 검증해볼 첫 사례가 필요했음.
> 클릭 영역이 이미지 위에서도 정상 동작하는지 미리 확인해야 나머지 방들도 같은 방식으로 안심하고
> 진행할 수 있음.

### 04. 게임 화면 해상도 확장

> [!NOTE]
> **HOW** — 원본 이미지 해상도(2752×1536)를 기준으로 화질이 깨지지 않는 최대 크기를 계산해 게임
> 프레임을 480px → 1440px로 확장. 이후 화면이 낮은 모니터에서 씬 영역이 프레임 높이를 넘어 이동
> 버튼과 소지품 바가 잘려서 안 보이는 문제를 발견해, 씬 영역만 유동적으로 줄어들도록 레이아웃 구조를
> 수정.

> [!IMPORTANT]
> **WHY** — 클릭 영역이 좁으면 실제 플레이어가 불편할 거라는 우려와, 원본 이미지 해상도를 다 활용하지
> 못하고 있다는 점 때문에 확장을 결정함. 화면 크기가 커져도 이동·소지품 같은 필수 UI는 항상 보여야
> 실사용성이 깨지지 않음.

### 05. 비상등 깜빡임 연출 추가

> [!NOTE]
> **HOW** — 이미지 위에 반투명 빨간 방사형 그라디언트를 `mix-blend-mode: screen`으로 얹고, 3.4초
> 주기로 불규칙하게 밝기가 변하는 CSS 애니메이션을 구현.

> [!IMPORTANT]
> **WHY** — 정전된 야간 학원이라는 설정을 정지 이미지만으로는 다 표현하기 어려워서, 별도 영상 자산
> 없이 최소한의 코드만으로 분위기를 강화하기 위함. 영상 자산 제작에 비용을 쓰기 전에 저비용 방식으로
> 먼저 만족스러운지 검증.

### 06. BGM 파일 연결 및 재생 확인

> [!NOTE]
> **HOW** — 업로드받은 `bgm.mp3`를 `Sound` 폴더에 배치하고, 기존에 준비해둔 오디오 재생 로직과
> 연결해 최초 클릭 시 자동재생되는지, 음소거 버튼이 정상 동작하는지 실제 브라우저에서 확인.

> [!IMPORTANT]
> **WHY** — 브라우저는 사용자 상호작용 전 자동재생을 막기 때문에, 실제 파일로 재생 흐름을 검증해야
> 기능이 완성됐다고 확신할 수 있었음.

### 07. 테스트용 자유 이동 모드 추가

> [!NOTE]
> **HOW** — `DEBUG_FREE_ROAM` 플래그를 추가해, 켜두면 퍼즐 클릭이 전부 무시되고 모든 문·서랍이
> 항상 열린 것처럼 처리되도록 분기 처리. 끄면 원래의 정상 플레이 흐름으로 그대로 복귀.

> [!IMPORTANT]
> **WHY** — 그래픽·레이아웃을 검증하는 단계에서는 매번 퍼즐을 풀며 이동하는 게 비효율적이라, 퍼즐
> 로직 자체는 건드리지 않고 손쉽게 켜고 끌 수 있는 테스트 우회 경로가 필요했음.

### 08. 인포데스크 배경 이미지 적용

> [!NOTE]
> **HOW** — 변환된 아카데미 프론트(인포데스크) 이미지를 실제 게임에 적용. 방문자 명단·서랍 히트박스
> 위치는 아직 정하지 않아 `TODO` 주석만 남기고 보류.

> [!IMPORTANT]
> **WHY** — 이미지 자산이 완성되는 대로 바로 반영해 시각적 진행 상황을 눈으로 계속 확인하기 위함.
> 퍼즐 위치 결정에 시간이 걸리더라도 배경 교체 자체를 미룰 필요는 없었음.

---

## 2026-09-14

### 01. 남은 5개 방 이미지 전체 적용 완료

> [!NOTE]
> **HOW** — 본 강의실, 스터디룸, 화장실, 복도(우), 엘리베이터 앞, 엘리베이터 내부까지 변환된 이미지를
> 순서대로 적용해 8개 방 전체를 이미지 기반으로 전환 완료. 파일명이 실제 코드와 다르게 저장된 경우(한글
> 파일명, 확장자 불일치 등)를 발견해 파일명 규칙을 맞추거나 코드 쪽 참조를 수정.

> [!IMPORTANT]
> **WHY** — 그래픽 자산 파이프라인의 최종 목표였던 "전체 방 이미지 전환"을 완결짓기 위함. 파일명은
> 한글·특수문자가 섞이면 이후 GitHub Pages 같은 실제 웹 배포 환경에서 인코딩 문제로 깨질 수 있어,
> 발견 즉시 영문 규칙으로 통일함.

### 02. 이동 동선 좌/우 구조로 최종 확정 및 막다른 길 수정

> [!NOTE]
> **HOW** — 사용자가 정리한 이동 경로 명세에 맞춰 전체 방 연결 구조를 재구성. 인포데스크 · 스터디룸을
> 하나의 지선으로, 복도(우) · 엘리베이터 앞 · 화장실을 다른 지선으로 분리하고, 복도(좌)를 중심 허브로
> 재배치. 검증 과정에서 "엘리베이터 앞 → 화장실"만 있고 되돌아오는 경로가 없어 그 사이에 갇히는
> 막다른 구조를 발견해 "엘리베이터 앞 ↔ 복도(우)" 복귀 경로를 추가.

> [!IMPORTANT]
> **WHY** — 사람이 직접 손으로 그래프를 설계하면 왕복 경로 하나를 빠뜨리기 쉬움. 실제로 전체 8개 방을
> 자동으로 순회하며 모든 연결에 되돌아오는 경로가 있는지 검증하는 절차를 거쳐, 플레이어가 특정 구간에
> 갇히는 치명적 버그를 배포 전에 미리 차단함.

### 03. 이미지 표시 방식 수정 (잘라내기 → 전체 보이기)

> [!NOTE]
> **HOW** — 배경 이미지 표시 방식을 `object-fit: cover`(화면에 꽉 채우려고 넘치는 부분을 잘라냄)에서
> `object-fit: contain`(그림 전체를 항상 다 보여주고 남는 공간은 여백 처리)으로 변경. 여백은 어두운
> 배경색으로 채워 다크 테마와 자연스럽게 어울리도록 처리.

> [!IMPORTANT]
> **WHY** — 화면 비율과 이미지 비율이 정확히 일치하지 않으면 기존 방식은 그림의 위아래 또는 좌우를
> 잘라내는데, 공들여 구도를 잡은 배경 그림에서 중요한 요소(문, 간판 등)가 잘려 보이는 문제가 있었음.
> 약간의 여백이 생기더라도 그림 전체가 항상 온전히 보이는 쪽이 완성도 면에서 더 나음.

### 04. 씬 영역 레이아웃 버그 근본 수정 (줌 배율에 따라 하단이 잘리던 문제)

> [!NOTE]
> **HOW** — 브라우저 확대 배율(80% vs 100%)에 따라 화면 하단이 잘리는 증상을 재현·조사한 끝에,
> 전체 게임 카드(`#frame`)에 `max-height`만 있고 명확한 `height` 값이 없어 내부 씬 영역의 flex
> 계산이 배율에 따라 반올림 오차로 흔들리는 것이 원인임을 특정. `height:94vh`를 명시적으로 추가해
> 근본 원인을 해결.

> [!IMPORTANT]
> **WHY** — 눈으로 보이는 증상(하단 잘림)만 대응하면 재발할 수 있어, 여러 화면 크기와 배율 조합으로
> 실제 레이아웃 수치를 직접 측정해 정확한 원인을 특정한 뒤 수정함. `max-height`만 있고 `height`가
> 없는 flex 컨테이너는 자식의 `flex-grow` 계산이 브라우저·배율에 따라 불안정해질 수 있다는 것을
> 확인한 사례.

---

<!--
다음 기록을 추가할 때는 아래 형식을 복사해서 위쪽(최신순)에 붙여넣으면 됩니다.

## YYYY-MM-DD


### 작업 제목

> [!NOTE]
> **HOW** — (무엇을 어떻게 했는지)

> [!IMPORTANT]
> **WHY** — (왜 그렇게 했는지)
-->
